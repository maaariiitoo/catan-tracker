# Python 3.7 o mas nuevo. Y nada mas: cero dependencias, ningun pip install.
#
#     minimo    Python 3.7
#     probado   Python 3.14.6
#
# El minimo no es una promesa a ojo: `db/pruebas.py` analiza todo el codigo
# haciendose el de la 3.7, y si alguien cuela un walrus o un `int | None` la
# prueba salta y avisa de que el requisito ha subido.
#
# NADA QUE INSTALAR, medido y no supuesto: bloqueando a la fuerza cv2, numpy,
# torch, pillow, mss y pytesseract, los 17 modulos que se publican cargan
# igual y las vistas se montan. Todo lo que esto usa ya viene con Python:
# sqlite3, http.server, json, urllib, zipfile, hashlib.
#
#     py panel.py --simple
#
# El mod tampoco pide instalar nada a mano: BepInEx se lo baja el boton
# (comprobando su huella SHA-256) y el plugin lo compila el csc.exe que ya
# viene con Windows.
#
# La otra mitad --leer el tablero de la pantalla-- si necesita opencv-python,
# numpy, pillow, mss, pytesseract y torch, y Tesseract OCR aparte. Esa mitad
# NO va en el repositorio todavia; el .gitignore dice por que. Cuando vuelva,
# vuelven estas lineas aqui abajo, y no antes: un `pip install -r` de 2,5 GB
# que ademas puede fallar hace pensar que el proyecto no arranca. Arranca.
